class TestServer():
    hostname = self.hostname
	managementIp = self.managementIp
	memory = self.memory
	name = self.name
	os = self.os
	platform = self.platform
	state = self.state
	ownerIp = self.ownerIp


	def __init__(self, hostname, managementIp, memory, name, os, platform, state, ownerIp):
		hostname = self.hostname
		managementIp = self.managementIp
		memory = self.memory
		name = self.name
		os = self.os
		platform = self.platform
		state = self.state
		ownerIp = self.ownerIp

